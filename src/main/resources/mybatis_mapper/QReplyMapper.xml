<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.boot_ex_board_from_servlet.mapper.QReplyMapper">

    <insert id="insert" parameterType="com.example.mentoring_project.domain.QReplyVO" useGeneratedKeys="true" keyProperty="qRno">
        INSERT INTO qnacomment (qRno, qBoardNo, id, content, nickname)
        VALUES (#{qRno}, #{qBoardNo}, #{id}, #{content}, #{nickname})
    </insert>

    <select id="selectListOfBoard" resultType="com.example.mentoring_project.domain.QReplyVO">
        SELECT * FROM qnacomment WHERE qBoardNo = #{qBoardNo} ORDER BY qRno ASC limit #{skip}, #{size}
    </select>

    <select id="getCount" resultType="int">
        SELECT COUNT(*) FROM qnacomment where qRno = #{qRno}
    </select>

    <select id="selectOne" resultType="com.example.mentoring_project.domain.QReplyVO">
        SELECT * FROM qnacomment WHERE qRno = #{qRno}
    </select>

    <update id="update">
        UPDATE qnacomment SET content = #{content} where qRno = #{qRno}
    </update>

    <delete id="delete">
        DELETE FROM qnacomment WHERE qRno = #{qRno}
    </delete>

</mapper>